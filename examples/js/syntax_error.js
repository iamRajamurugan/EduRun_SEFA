console.log("This will cause a syntax error");

// Missing closing brace
const obj = {
    name: "test",
    value: 42
    // Missing closing brace here

console.log("This line will never be reached");

// Another syntax error - missing semicolon and quote
const message = "Unclosed string
console.log(message);
