console.log("Hello, World from JavaScript!");
console.log("🟨 JavaScript Docker Compiler is working!");

// Show some basic JavaScript features
const message = "JavaScript in Docker!";
console.log(`Dynamic message: ${message}`);

// Array operations
const numbers = [1, 2, 3, 4, 5];
const sum = numbers.reduce((acc, num) => acc + num, 0);
console.log(`Sum of [${numbers.join(", ")}] = ${sum}`);

// Modern JavaScript
const greet = (name) => `Hello, ${name}!`;
console.log(greet("Docker"));
